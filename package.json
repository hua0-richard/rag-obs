{
  "name": "rag-obs",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "infra": "docker compose up --build",
    "ollama": "OLLAMA_HOST=localhost:11434 ollama serve",
    "dev:full": "pnpm run models:download:docker && pnpm run infra & pnpm run ollama",
    "models:download": "python backend/scripts/download_models.py",
    "models:download:docker": "docker compose run --rm -e HF_HUB_OFFLINE=0 -e TRANSFORMERS_OFFLINE=0 api python scripts/download_models.py"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.15.0"
}
